{
  "name": "my-website",
  "version": "1.0.0",
  "description": "Personal website with delivery dashboard",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
